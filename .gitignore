# Dependencies
node_modules/

# Generated artifacts (rebuilt by tooling — never committed)
test-results/
reports/
playwright-report/
/playwright/.cache/

# NOTE: skills/ is the source of truth and IS committed.
#       .claude/skills/ is a committed mirror (regenerated by `npm run sync:skills`)
#       so native Claude Code skill auto-loading works on a fresh clone.

# Logs / OS cruft
*.log
.DS_Store
Thumbs.db
